"use client";import {useState} from 'react';import {Button} from '@/components/ui/button';export function WalletTopUp(){const [amount,setAmount]=useState('');const [message,setMessage]=useState('');async function submit(){const r=await fetch('/api/wallet',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount})});const j=await r.json();setMessage(r.ok?(j.configured?'Paystack checkout initialization ready.':'Connect Paystack credentials to enable checkout.'):(j.error||'Unable to start top-up'))}return <div className="mt-6 flex max-w-md flex-col gap-3"><label className="text-sm font-medium">Top up amount<input type="number" min="1" value={amount} onChange={e=>setAmount(e.target.value)} className="mt-2 w-full rounded-xl border bg-background px-4 py-3" placeholder="100000"/></label><Button onClick={submit}>Start secure top-up</Button>{message&&<p className="text-sm text-muted-foreground">{message}</p>}</div>}
