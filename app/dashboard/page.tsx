import { createClient } from '@/lib/supabase/server'
import { LayoutDashboard, ShieldCheck, WalletCards, BriefcaseBusiness } from 'lucide-react'

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  return <main className="min-h-screen bg-muted/35"><header className="border-b border-border bg-card"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><a href="/" className="font-mono text-sm font-bold tracking-widest text-primary">TRAVELTRADE / APP</a><div className="flex items-center gap-3 text-sm"><span className="hidden text-muted-foreground sm:inline">{user?.email ?? 'Guest preview'}</span><a href="/" className="rounded-lg border border-border px-3 py-2">Marketplace</a></div></div></header><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div><p className="font-mono text-xs font-bold uppercase tracking-widest text-primary">Workspace</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Good morning{user?.email ? '' : ', traveler'}.</h1><p className="mt-2 text-muted-foreground">Your orders, proposals, and protected funds in one place.</p></div><div className="mt-8 grid gap-5 md:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><LayoutDashboard className="size-5 text-primary" /><p className="mt-6 text-sm text-muted-foreground">Active orders</p><p className="mt-1 text-3xl font-semibold">0</p></div><div className="rounded-2xl border border-border bg-card p-5"><WalletCards className="size-5 text-primary" /><p className="mt-6 text-sm text-muted-foreground">In escrow</p><p className="mt-1 font-mono text-3xl font-semibold">₦0</p></div><div className="rounded-2xl border border-border bg-card p-5"><ShieldCheck className="size-5 text-primary" /><p className="mt-6 text-sm text-muted-foreground">Account status</p><p className="mt-1 text-xl font-semibold">Protected</p></div></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.4fr_0.6fr]"><section className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Your workspace</h2><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">No activity yet</span></div><div className="mt-10 flex flex-col items-center justify-center py-8 text-center"><BriefcaseBusiness className="size-10 text-muted-foreground/40" /><h3 className="mt-4 font-semibold">Ready for your first brief?</h3><p className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">Browse verified services or start a request for proposal from a trusted travel partner.</p><a href="/#marketplace" className="mt-5 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Browse marketplace</a></div></section><aside className="rounded-2xl border border-border bg-primary p-6 text-primary-foreground"><p className="font-mono text-xs uppercase tracking-widest text-primary-foreground/70">Trust layer</p><h2 className="mt-4 text-2xl font-semibold">Every decision, documented.</h2><p className="mt-3 text-sm leading-6 text-primary-foreground/75">Milestones and escrow keep expectations clear for both sides.</p></aside></div></div></main>
}
