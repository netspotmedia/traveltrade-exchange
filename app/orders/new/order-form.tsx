"use client"
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
export default function OrderForm({serviceId,agencyId}:{serviceId:string|null;agencyId:string|null}){const router=useRouter();const [title,setTitle]=useState('');const [amount,setAmount]=useState('');const [message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMessage('Creating order…');const response=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,totalAmount:amount,serviceId,agencyId,idempotencyKey:crypto.randomUUID()})});const result=await response.json();if(!response.ok)return setMessage(result.error||'Unable to create order');setMessage('Order created.');router.push(`/dashboard/orders/${result.order.id}`)}return <main className="min-h-screen bg-background px-6 py-10"><form onSubmit={submit} className="mx-auto flex max-w-xl flex-col gap-5 rounded-3xl border bg-card p-8"><div><p className="text-sm font-semibold text-primary">New order</p><h1 className="mt-2 text-3xl font-semibold">Tell the agency what you need</h1></div><label className="flex flex-col gap-2 text-sm font-medium">Order title<input required value={title} onChange={e=>setTitle(e.target.value)} className="rounded-xl border bg-background px-4 py-3" placeholder="Airport transfer for Lagos team" /></label><label className="flex flex-col gap-2 text-sm font-medium">Proposed budget (NGN)<input required min="1" type="number" value={amount} onChange={e=>setAmount(e.target.value)} className="rounded-xl border bg-background px-4 py-3" /></label><Button type="submit">Create protected order</Button>{message&&<p className="text-sm text-muted-foreground">{message}</p>}</form></main>}
