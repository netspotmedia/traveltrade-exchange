"use client"
import { useState } from 'react'
import { Button } from '@/components/ui/button'

export default function OnboardingPage(){ const [name,setName]=useState(''); const [file,setFile]=useState<File|null>(null); const [status,setStatus]=useState('')
async function submit(e:React.FormEvent){e.preventDefault();setStatus('Saving onboarding details…');const r=await fetch('/api/onboarding',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,documentType:'business_registration',fileName:file?.name})});const j=await r.json();setStatus(r.ok?'Application submitted for review.':j.error||'Unable to submit.')} return <main className="min-h-screen bg-background px-6 py-10"><form onSubmit={submit} className="mx-auto flex max-w-2xl flex-col gap-6 rounded-3xl border bg-card p-8"><div><p className="text-sm font-semibold text-primary">Agency verification</p><h1 className="mt-2 text-3xl font-semibold">Complete your KYB profile</h1><p className="mt-2 text-muted-foreground">Submit accurate business information and a registration document for review.</p></div><label className="flex flex-col gap-2 text-sm font-medium">Legal business name<input required value={name} onChange={e=>setName(e.target.value)} className="rounded-xl border bg-background px-4 py-3" /></label><label className="flex flex-col gap-2 text-sm font-medium">Registration document<input required type="file" accept=".pdf,.png,.jpg,.jpeg" onChange={e=>setFile(e.target.files?.[0]||null)} className="rounded-xl border bg-background p-3" /></label><Button type="submit">Submit for verification</Button>{status&&<p className="text-sm text-muted-foreground">{status}</p>}</form></main> }
